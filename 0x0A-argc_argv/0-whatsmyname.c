#include <stdio.h>
/**
 * main - prints
 * @argc: the no of arg
 * @argv: array
 * Return: always 0
 */
int main(int __attribute((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[]);

	return (0);
}

