#include <stdio.h>

/**
 * main - program to calculate numbers
 * Return: 0 if it is executable
 */
int main(void)
{
	int ch;

	for (ch = 48; ch <= 57; ch++)
	{
		putchar(ch);
	}
	putchar(10)

		return (0);
}
